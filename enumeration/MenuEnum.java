package enumeration;

// Перечисление режимов работы программы
public enum MenuEnum {
    addAnimal, show, learnCommands, printAnimalList, sortByNumber, sortByBirthDate, printCount, remove, exit
}
